name: Despliegue continuo a GitHub Pages

# 🔹 CUÁNDO se ejecutará
on:
  push:
    branches: [ "main" ]   # Se ejecuta al hacer push a main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Descargar tu código
      - name: Checkout del código
        uses: actions/checkout@v3

      # 2️⃣ Configurar Node.js
      - name: Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # 3️⃣ Instalar dependencias
      - name: Instalar dependencias
        run: npm install

      # 4️⃣ Ejecutar build (si es proyecto React o Vue)
      - name: Generar build de producción
        run: |
          if [ -f package.json ] && grep -q '"build"' package.json; then
            npm run build
          else
            echo "No build step needed"
          fi

      # 5️⃣ Desplegar en GitHub Pages
      - name: Desplegar en GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # Cambia la carpeta según tu proyecto:
          # React → build
          # Vue → dist
          # Estático → ./public o ./ (raíz)
          publish_dir: ./build


